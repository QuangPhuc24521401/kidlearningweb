node_modules/
.env
.vercel/

# Local secrets (do not commit)
secrets/
**/secrets/
secrets/*.js
**/secrets/*.js
# Stub shipped so local / Firebase static hosting không 404; bản production có thể dùng Vercel rewrite → /api/tts-config
!public/secrets/
!public/secrets/tts.config.js